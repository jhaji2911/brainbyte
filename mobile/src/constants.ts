import { Byte, LeaderboardUser } from "./types";

// Fallback content — empty. All content comes from the v2 curation agent.
export const BYTES: Byte[] = [];

export const LEADERBOARD: LeaderboardUser[] = [];
